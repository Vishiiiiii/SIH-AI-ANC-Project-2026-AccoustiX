# Core ML
torch>=2.0.0
torchaudio>=2.0.0
numpy>=1.24.0

# Audio Processing
soundfile>=0.12.0
scipy>=1.10.0
sounddevice>=0.4.6

# Optional: accelerates the one-time dual-mic dataset precompute. The live
# NLMS implementation does not require it.
numba>=0.61.0

# Deployment
onnx>=1.14.0
onnxruntime>=1.15.0

# Utilities
pyyaml>=6.0
tqdm>=4.65.0
matplotlib>=3.7.0

# Evaluation Metrics
pesq>=0.0.4
pystoi>=0.3.3
